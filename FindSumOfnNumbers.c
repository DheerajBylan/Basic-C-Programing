#include <stdio.h>
int main(){

  int sum = 0;
  int n;

  printf("enter any number : ");
  scanf("%d",&n);

  sum = (n/2)*(n+1)
  /*
  previous programe take order of n seconds 
  by using math formula sum = (n/2)*(2a + (n-1)*d) 
  where a is starting value of AP (Arithmetic progression) and d is Diffrence between every element 
  A.P (Arithmetic progression) -> 1,2,3,4,5,6, ...
  */


  printf("the sum is : %d",sum);
  return 0;
}
